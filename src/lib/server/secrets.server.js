// server.js or any js module that has server.js on the name is a dedicated module used only in server load function or +page.server.js
// same goes to the modules inside a server folder
// you can do this anywhere inside the routes folder, just add .server.js in the file name like this

export const greetings = 'Hi, Im zedrick';
